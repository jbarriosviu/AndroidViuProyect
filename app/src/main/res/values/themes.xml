<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PsicologiaApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PsicologiaApp" parent="Base.Theme.PsicologiaApp" />

    <style name="SubTituloMenu">
        <item name="textAllCaps">true</item>
        <item name="android:textColor">@color/canal_subtitle_text</item>
    </style>

</resources>